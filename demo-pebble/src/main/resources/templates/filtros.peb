<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Pebble - filtros</title>
</head>
<body>
    <h1>Ola, {{ nome | upper }}</h1>

    <h1>lista</h1>
    <ul>
        {% for item in lista %}
            <li>{{ item | lower }}</li>
        {% endfor %}
    </ul>

    <p>Valor original: {{ valor }}</p>
    <p>Arredondado (0 casas): {{ valor | numberformat("#.##")  }}</p>
</body>
</html>